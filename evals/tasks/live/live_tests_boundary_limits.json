{
  "contract_version": "1.0",
  "task_id": "live_tests_boundary_limits",
  "suite": "live",
  "description": "Add tests for the inclusive boundaries of a clamp.",
  "fixture": "live_clamp",
  "user_request": "test_clamp.py only covers a value in the middle of the range. Add tests for values below, above, and exactly on each boundary, without changing clamp.py. Run the tests.",
  "mode": "default",
  "tags": [
    "new_tests",
    "verification"
  ],
  "trust": "trusted",
  "approval_policy": {
    "edit": "accept",
    "tool": "accept"
  },
  "provider_mode": "live",
  "profile": {
    "provider": "anthropic",
    "model": "claude-sonnet-5",
    "context_window": 200000,
    "max_output_tokens": 8192,
    "input_per_1m": 3.0,
    "output_per_1m": 15.0
  },
  "budgets": {
    "max_model_calls": 40,
    "max_tokens": 300000,
    "max_cost": 0.5,
    "wall_time_seconds": 300
  },
  "checks": {
    "test_commands": [
      [
        "python3",
        "-m",
        "pytest",
        "-q"
      ]
    ],
    "required_events": [
      "edit.applied",
      "usage.recorded",
      "final"
    ],
    "required_audit_events": [
      "tool.started",
      "tool.finished",
      "session.final"
    ],
    "minimum_compactions": 0,
    "expected_approvals": {
      "edit": "accept"
    },
    "mutations": [
      {
        "path": "clamp.py",
        "old_text": "    if value < low:\n        return low",
        "new_text": "    if value < low:\n        return value"
      },
      {
        "path": "clamp.py",
        "old_text": "    if value > high:\n        return high",
        "new_text": "    if value > high:\n        return high + 1"
      }
    ],
    "files": [
      {
        "path": "test_clamp.py",
        "exists": true
      }
    ],
    "allowed_changed_paths": [
      "test_clamp.py"
    ],
    "forbidden_changed_paths": [
      "clamp.py"
    ]
  }
}
