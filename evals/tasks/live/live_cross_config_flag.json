{
  "contract_version": "1.0",
  "task_id": "live_cross_config_flag",
  "suite": "live",
  "description": "Thread a new configuration flag from the config object into the service.",
  "fixture": "live_config_flag",
  "user_request": "Add a `strict` flag to Config, defaulting to False. When it is True, configure must raise ValueError on an unknown override key instead of ignoring it. Run the tests.",
  "mode": "default",
  "tags": [
    "cross_file",
    "edit",
    "verification"
  ],
  "trust": "trusted",
  "approval_policy": {
    "edit": "accept",
    "tool": "accept"
  },
  "provider_mode": "live",
  "profile": {
    "provider": "anthropic",
    "model": "claude-sonnet-5",
    "context_window": 200000,
    "max_output_tokens": 8192,
    "input_per_1m": 3.0,
    "output_per_1m": 15.0
  },
  "budgets": {
    "max_model_calls": 40,
    "max_tokens": 300000,
    "max_cost": 0.5,
    "wall_time_seconds": 300
  },
  "checks": {
    "test_commands": [
      [
        "python3",
        "-m",
        "pytest",
        "-q"
      ]
    ],
    "required_events": [
      "edit.applied",
      "usage.recorded",
      "final"
    ],
    "required_audit_events": [
      "tool.started",
      "tool.finished",
      "session.final"
    ],
    "minimum_compactions": 0,
    "expected_approvals": {
      "edit": "accept"
    },
    "files": [
      {
        "path": "config.py",
        "contains": [
          "strict"
        ]
      },
      {
        "path": "service.py",
        "contains": [
          "strict"
        ]
      }
    ],
    "allowed_changed_paths": [
      "config.py",
      "service.py"
    ],
    "forbidden_changed_paths": [
      "test_config_flag.py"
    ]
  }
}
