{
  "contract_version": "1.0",
  "task_id": "live_tests_idempotence",
  "suite": "live",
  "description": "Add tests for the documented idempotence of tag addition.",
  "fixture": "live_tags",
  "user_request": "add_tag documents that adding the same tag twice changes nothing, and that is untested. Add tests covering it, without changing tags.py. Run the tests.",
  "mode": "default",
  "tags": [
    "new_tests",
    "verification"
  ],
  "trust": "trusted",
  "approval_policy": {
    "edit": "accept",
    "tool": "accept"
  },
  "provider_mode": "live",
  "profile": {
    "provider": "anthropic",
    "model": "claude-sonnet-5",
    "context_window": 200000,
    "max_output_tokens": 8192,
    "input_per_1m": 3.0,
    "output_per_1m": 15.0
  },
  "budgets": {
    "max_model_calls": 40,
    "max_tokens": 300000,
    "max_cost": 0.5,
    "wall_time_seconds": 300
  },
  "checks": {
    "test_commands": [
      [
        "python3",
        "-m",
        "pytest",
        "-q"
      ]
    ],
    "required_events": [
      "edit.applied",
      "usage.recorded",
      "final"
    ],
    "required_audit_events": [
      "tool.started",
      "tool.finished",
      "session.final"
    ],
    "minimum_compactions": 0,
    "expected_approvals": {
      "edit": "accept"
    },
    "mutations": [
      {
        "path": "tags.py",
        "old_text": "    if tag in tags:\n        return list(tags)\n",
        "new_text": ""
      }
    ],
    "files": [
      {
        "path": "test_tags.py",
        "exists": true
      }
    ],
    "allowed_changed_paths": [
      "test_tags.py"
    ],
    "forbidden_changed_paths": [
      "tags.py"
    ]
  }
}
