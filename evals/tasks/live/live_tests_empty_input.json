{
  "contract_version": "1.0",
  "task_id": "live_tests_empty_input",
  "suite": "live",
  "description": "Add tests covering the documented empty-input behaviour.",
  "fixture": "live_stats",
  "user_request": "test_stats.py never exercises the empty-input case that stats.py documents. Add tests covering it for both functions, without changing stats.py. Run the tests.",
  "mode": "default",
  "tags": [
    "new_tests",
    "verification"
  ],
  "trust": "trusted",
  "approval_policy": {
    "edit": "accept",
    "tool": "reject"
  },
  "provider_mode": "live",
  "profile": {
    "provider": "anthropic",
    "model": "claude-sonnet-5",
    "context_window": 200000,
    "max_output_tokens": 8192,
    "input_per_1m": 3.0,
    "output_per_1m": 15.0
  },
  "budgets": {
    "max_model_calls": 40,
    "max_tokens": 300000,
    "max_cost": 0.5,
    "wall_time_seconds": 300
  },
  "checks": {
    "test_commands": [
      [
        "python3",
        "-m",
        "pytest",
        "-q"
      ]
    ],
    "required_events": [
      "edit.applied",
      "usage.recorded",
      "final"
    ],
    "required_audit_events": [
      "tool.started",
      "tool.finished",
      "session.final"
    ],
    "minimum_compactions": 0,
    "expected_approvals": {
      "edit": "accept"
    },
    "mutations": [
      {
        "path": "stats.py",
        "old_text": "        return 0.0\n    return sum(values)",
        "new_text": "        return 1.0\n    return sum(values)"
      },
      {
        "path": "stats.py",
        "old_text": "        return 0.0\n    return max(values)",
        "new_text": "        return -1.0\n    return max(values)"
      }
    ],
    "files": [
      {
        "path": "test_stats.py",
        "exists": true
      }
    ],
    "allowed_changed_paths": [
      "test_stats.py"
    ],
    "forbidden_changed_paths": [
      "stats.py"
    ]
  }
}
