{
  "contract_version": "1.0",
  "task_id": "coord_units",
  "suite": "live_coordinated",
  "description": "A unit change every consumer must absorb in a different way.",
  "fixture": "coord_units",
  "user_request": "test_timing.py fails: durations should be milliseconds, not seconds. Make the change everywhere it is needed, then run the tests.",
  "mode": "default",
  "tags": [
    "coordinated_change",
    "hard"
  ],
  "trust": "trusted",
  "approval_policy": {
    "edit": "accept",
    "tool": "accept"
  },
  "provider_mode": "live",
  "profile": {
    "provider": "anthropic",
    "model": "claude-sonnet-5",
    "context_window": 200000,
    "max_output_tokens": 8192,
    "input_per_1m": 3.0,
    "output_per_1m": 15.0
  },
  "budgets": {
    "max_model_calls": 60,
    "max_tokens": 600000,
    "max_cost": 1.0,
    "wall_time_seconds": 600
  },
  "checks": {
    "test_commands": [
      [
        "python3",
        "-m",
        "pytest",
        "-q"
      ]
    ],
    "allowed_changed_paths": [
      "timing.py",
      "report.py",
      "alerts.py",
      "budget.py"
    ],
    "forbidden_changed_paths": [
      "test_timing.py"
    ],
    "required_events": [
      "edit.applied",
      "usage.recorded",
      "final"
    ],
    "required_audit_events": [
      "tool.started",
      "tool.finished",
      "session.final"
    ],
    "minimum_compactions": 0,
    "expected_approvals": {
      "edit": "accept"
    }
  }
}
